import React from 'react';

const drawerToggle = props => (
    <div onClick={props.click}>
        <div>--</div>
        <div>--</div>
        <div>--</div>
    </div>
)

export default drawerToggle;